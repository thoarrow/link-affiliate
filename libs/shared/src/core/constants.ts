export enum AppMode {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  PREVIEW = 'preview',
}

export const MAX_PAGE_ITEMS = 20;
export const MIN_PAGE_ITEMS = 1;
export const MIN_OFFSET = 0;
